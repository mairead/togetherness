import React from 'react';
import Letter from '../components/Letter';

describe('Letter', () => {
  it('Should return the distance from the center of the letter', () => {

    // instance.getDistanceFromCenter(50);
    expect(getDistanceFromCenter).to.equal(0);
  });
});
